<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.busanbank.mapper.ChatSessionMapper">

    <resultMap id="ChatSessionResultMap" type="kr.co.busanbank.dto.chatting.ChatSessionDTO">
        <id     column="sessionId"      property="sessionId"/>
        <result column="userId"         property="userId"/>
        <result column="consultantId"   property="consultantId"/>
        <result column="inquiryType"    property="inquiryType"/>
        <result column="status"         property="status"/>
        <result column="priorityScore"  property="priorityScore"/>
        <result column="createdAt"      property="createdAt"/>
        <result column="updatedAt"      property="updatedAt"/>
        <result column="waitStartTime"  property="waitStartTime"/>
        <result column="chatStartTime"  property="chatStartTime"/>
        <result column="chatEndTime"    property="chatEndTime"/>
    </resultMap>

    <insert id="insertChatSession" parameterType="kr.co.busanbank.dto.chatting.ChatSessionDTO">
        <selectKey keyProperty="sessionId" resultType="int" order="BEFORE">
            SELECT CHAT_SESSION_SEQ.NEXTVAL FROM DUAL
        </selectKey>

        INSERT INTO ChatSession (
        sessionId,
        userId,
        inquiryType,
        status,
        priorityScore
        ) VALUES (
        #{sessionId},
        #{userId},
        #{inquiryType},
        #{status},
        #{priorityScore}
        )
    </insert>

    <select id="selectChatSessionById" resultMap="ChatSessionResultMap">
        SELECT
            sessionId,
            userId,
            consultantId,
            inquiryType,
            status,
            priorityScore,
            createdAt,
            updatedAt,
            waitStartTime,
            chatStartTime,
            chatEndTime
        FROM ChatSession
        WHERE sessionId = #{sessionId}
    </select>

    <update id="updateChatSession" parameterType="kr.co.busanbank.dto.chatting.ChatSessionDTO">
        UPDATE ChatSession
        SET userId        = #{userId},
            consultantId  = #{consultantId},
            inquiryType   = #{inquiryType},
            status        = #{status},
            priorityScore = #{priorityScore},
            waitStartTime = #{waitStartTime},
            chatStartTime = #{chatStartTime},
            chatEndTime   = #{chatEndTime},
            updatedAt     = #{updatedAt}
        WHERE sessionId     = #{sessionId}
    </update>

    <update id="updateChatSessionStatus">
        UPDATE ChatSession
        SET status    = #{status},
            updatedAt = #{updatedAt}
        WHERE sessionId = #{sessionId}
    </update>

    <select id="selectByStatus" resultMap="ChatSessionResultMap">
        SELECT
            sessionId,
            userId,
            consultantId,
            inquiryType,
            status,
            priorityScore,
            createdAt,
            updatedAt,
            waitStartTime,
            chatStartTime,
            chatEndTime
        FROM ChatSession
        WHERE status = #{status}
        ORDER BY createdAt ASC
    </select>

    <select id="selectByStatusAndConsultant" resultMap="ChatSessionResultMap">
        SELECT
            sessionId,
            userId,
            consultantId,
            inquiryType,
            status,
            priorityScore,
            createdAt,
            updatedAt,
            waitStartTime,
            chatStartTime,
            chatEndTime
        FROM ChatSession
        WHERE status = #{status}
          AND consultantId = #{consultantId}
        ORDER BY createdAt ASC
    </select>

    <update id="assignConsultantToSession">
        UPDATE ChatSession
        SET consultantId  = #{consultantId},
            status        = #{status},
            chatStartTime = SYSDATE,
            updatedAt     = SYSDATE
        WHERE sessionId     = #{sessionId}
    </update>

    <update id="closeChatSession">
        UPDATE ChatSession
        SET status      = #{status},
            chatEndTime = SYSDATE,
            updatedAt   = SYSDATE
        WHERE sessionId   = #{sessionId}
    </update>

    <select id="selectChattingSessionsWithUnread"
            resultType="kr.co.busanbank.dto.chatting.ChatSessionDTO">
        SELECT
        cs.*,
        (
        SELECT COUNT(*)
        FROM ChatMessage cm
        WHERE cm.sessionId = cs.sessionId
        AND cm.senderId != #{consultantId}
        AND cm.isRead = 0
        ) AS unreadCount
        FROM ChatSession cs
        WHERE cs.status       = 'CHATTING'
        AND cs.consultantId = #{consultantId}
        ORDER BY cs.chatStartTime DESC
    </select>

</mapper>
